export interface Student {
  id?: string; // object có thể có hoặc không có thuộc tính này cũng được
  name: string;
  age: number;
  mark: number;
  gender: string;
  createdAt?: number; // object có thể có hoặc không có thuộc tính này cũng được
  updateAt?: number; // object có thể có hoặc không có thuộc tính này cũng được
  city: string;
}
